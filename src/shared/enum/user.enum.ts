export enum Gender_Enum {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum User_Role_Enum {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  USER = 'user',
  SALE = 'sale',
}
